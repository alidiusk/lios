#ifndef FUNCTION_H
#define FUNCTION_H

// Sometimes we want to keep parameters in a function for later
// use and this allows us to avoid the 'unused parameter' compiler warning
#define UNUSED(x) (void)(x)

#endif
